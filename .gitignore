# Secrets — nunca commitar
.env
.env.local
.env.production

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
supabase/.temp/

# Cache incremental do TypeScript e lockfile de um gerenciador de pacotes
# que o projeto não usa (fonte de verdade é package-lock.json/npm) — nenhum
# dos dois deveria ter sido versionado.
*.tsbuildinfo
*.tsbuildinfo.txt
bun.lock.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/settings.local.json
