/// <reference types="vite/client" />

// Injetado pelo vite.config.ts (define) em tempo de build — timestamp ISO
// de quando este bundle foi gerado. Ver src/lib/versionCheck.ts.
declare const __APP_BUILD_TIME__: string;
